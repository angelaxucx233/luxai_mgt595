services:
  - type: web
    name: luxai-mgt595
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false # Render will prompt you to paste it — it is stored server-side only
